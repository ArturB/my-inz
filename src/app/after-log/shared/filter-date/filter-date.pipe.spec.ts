import { FilterDatePipe } from './filter-date.pipe';

describe('FilterDatePipe', () => {
  it('create an instance', () => {
    let pipe = new FilterDatePipe();
    expect(pipe).toBeTruthy();
  });
});
