import { FilterDateMaxPipe } from './filter-date-max.pipe';

describe('FilterDateMaxPipe', () => {
  it('create an instance', () => {
    let pipe = new FilterDateMaxPipe();
    expect(pipe).toBeTruthy();
  });
});
